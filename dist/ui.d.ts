/**
 * Inline HTML dashboard for bg-manager.
 * Returns a complete HTML page as a string — no external files needed.
 * Uses ansi_up from CDN for ANSI color rendering.
 */
export declare function getUiHtml(port: number): string;
